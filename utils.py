"""
Utility Function holder
@author: Alexandre Riou
@date: May 2024
"""

import numpy as np


def identity(image: np.ndarray) -> np.ndarray:
    """
    a function which returns the input image as is. Used as a placeholder for image modifying functions
    :param image: the input image as a numpy array
    :return: the input image as a numpy array
    """
    return image
